__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.DS_Store
*.egg-info/

# Generated experiment data. Summaries are small and are kept; per-item rows
# and raw traces are not.
data/*.jsonl
data/traces/
data/raw/
data/_quant/

# Frozen result sets ARE tracked: 32 rows is 6 KB, it is the only copy of many
# GPU-hours, and a summary derived from it is not a substitute for the rows.
!results/
